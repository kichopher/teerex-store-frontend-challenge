import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#131b35",
        },
        secondary: {
            main: "#00e676"
        }
    },
    // typography: {
    //     button: {
    //         textTransform: "none"
    //     }
    // },
});

export default theme;
